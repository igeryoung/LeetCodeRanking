[phases.setup]
nixPkgs = ["nodejs_23"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run db:migrate && npm run db:seed && npm run start"
